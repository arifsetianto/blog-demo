ping:
    type: presenter
    methods: [get]
    path: ping
    presenter: ping-presenter

auth:
    type: group
    children: _auth.yaml

user:
    type: group
    children: _user.yaml

post:
    type: group
    children: _post.yaml